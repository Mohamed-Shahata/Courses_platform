model Outbox{
  id String @id @default(uuid())

  event_type EVENT_TYPE
  payload Json

  status STATUS_OUTBOX @default(PENDING)

  retryCount Int @default(0)

  nextRetryAt DateTime @default(now())

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt
}
